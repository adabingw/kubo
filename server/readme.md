express backend on k8s.
connects to frontend users via socketio.
listens to pubsub topics that users are subscribed to.
updates frontend on message/data change.
manages firestore events and references.
